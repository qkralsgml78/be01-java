package day05_random;

import java.util.Iterator;
import java.util.Random;

public class Lotto {

	//정수가 들어 있는 배열 num_list가 매개변수로 주어집니다. 
	//num_list의 원소의 순서를 거꾸로 뒤집은 배열을 return하도록 solution 함수를 완성해주세요.
	
	
	

	public static void main(String[] args) {
		
		/*
		 * 
		 * 	//문제 2. 1~45 숫자 중에서 6개 추출(단, 같은 수 없어야 함)
		int a =6;
		Random rand = new Random();
		int random = 0;
		int[] Lnum = new int[a];
		
		for (int i = 0; i < a-1; i++) {
			random = rand.nextInt(45)+1;
			Lnum[i] = random;
			
			if(Lnum[i] == Lnum[i+1]) {
				Lnum[i+1] = rand.nextInt(45)+1;
				
				if(Lnum[i] == Lnum[i+1]) {
					Lnum[i+1] = rand.nextInt(45)+1;
				}
			}
		}
		for (int j = 0; j < a-1; j++) {
			System.out.print("로또"+(j+1)+"번호는 "+Lnum[j]+"\t");
		}
		
		*/
	}
}
